-- definitions/teste.sqlx
-- Replace the existing query with your new join query
config {
  type: "table",
  tags: [
    "hourly"
  ]
}

-- Perform the join operation
SELECT
  a.*
FROM
  `${constants.SRC_PROJECT_ID}.${constants.SRC_DATASET_ID}.mytable`